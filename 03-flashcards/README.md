# Spaced-Repetition Flashcards

```bash
python flashcards.py add "What is a closure?" "A function plus its lexical environment"
python flashcards.py study
python -m unittest discover -s tests -v
```

Cards use a simplified SM-2 scheduling algorithm. Domain logic is pure and
separate from JSON storage, making it deterministic and testable.

