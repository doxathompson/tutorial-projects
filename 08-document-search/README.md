# Local Document Search

Indexes `.txt` and `.md` files and ranks results with BM25. This is the core
algorithm behind a useful search engine, kept dependency-free and readable.

```bash
python search.py ~/notes "python testing"
python -m unittest discover -s tests -v
```

